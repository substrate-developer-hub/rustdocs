initSidebarItems({"fn":[["gcd","Helper gcd function used in Rational128 implementation."],["multiply_by_rational","Safely and accurately compute `a * b / c`. The approach is:   - Simply try `a * b / c`.   - Else, swap the operations (divide first) if `a > c` (division is possible) and `b <= c`     (overflow cannot happen)   - At any point, given an overflow or accuracy loss, return an Error."],["multiply_by_rational_best_effort","Performs [`multiply_by_rational`]. In case of failure, if `b < c` it tries to approximate the ratio into a perquintillion and return a lossy result. Otherwise, a best effort approach of shifting both b and c is performed until multiply_by_rational can work."]]});